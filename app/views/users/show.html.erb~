<h3><strong> <%= @user.name %> </strong></h3>

<p><strong>Email: </strong>
<%= @user.email if @user.email %></p>

<p><strong>Address: </strong>
<%= @user.address if @user.address %></p>
</br>

<strong>Rides <%= @user.name %> is a Passenger On</strong>
<table>
<% @user_sitting_rides.each do |user_sitting_ride| %>
<tr><td>
  Ride: <%=link_to user_sitting_ride.id, user_sitting_ride %></td>
  <td><%= link_to 'Cancel Seat', user_sitting_ride, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr> 
<% end %>
</table>
</br>

<strong>Rides <%= @user.name %> is a Driver On</strong>
<table>
<% @user_driving_rides.each do |user_driving_ride| %>
<tr><td>
  Ride: <%=link_to user_driving_ride.id, user_driving_ride %></td>
  <td><%= link_to 'Cancel Drive', user_driving_ride, method: :delete, data: { confrim: 'Are you sure?' } %></td>
  </tr> 
<% end %>
</table>
